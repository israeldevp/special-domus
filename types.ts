export interface FAQItem {
  question: string;
  answer: string;
}

export interface FeatureItem {
  title: string;
  description: string;
  image: string;
}

export interface TestimonialData {
  quote: string;
  author: string;
  role?: string;
}